

import 'package:flutter/material.dart';

class NovoItemView extends StatelessWidget {
  const NovoItemView({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('novo item'),
    );
  }
}